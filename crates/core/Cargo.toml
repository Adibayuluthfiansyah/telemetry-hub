[package]
name = "telemetry_core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { workspace = true }
chrono = { version = "0.4", features = ["clock", "serde"] }
uuid = { version = "1", features = ["v4", "serde"] }

[dev-dependencies]
serde_json = "1"
