[package]
name = "telemetry_core"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", features = ["clock"] }
uuid = { version = "1", features = ["v4"] }
serde = { workspace = true }
